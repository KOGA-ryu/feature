# Right Inspector

`ui.right_inspector` is the first demo feature in the Rust feature lab.

Purpose:
a persistent right-side detail panel for inspecting the currently selected
object, its metadata, and linked artifacts.

Minimal demo scope:

- show selected object context
- list linked artifacts
- surface contextual actions
- keep metadata visible beside the main workspace
