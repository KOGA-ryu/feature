# Decisions

## 2026-04-26

- Decision: split the feature library into its own repo.
- Reason: the wiki tooling repo is not the right long-term home for Dex-facing reusable feature infrastructure.
- Alternatives rejected: keep the feature library embedded in `wiki`; start with a visual browser UI.
- Reversal condition: reverse only if a separate repo blocks reuse more than it helps.
