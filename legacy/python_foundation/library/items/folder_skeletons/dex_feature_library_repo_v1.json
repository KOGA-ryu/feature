{
  "app_archetypes": [
    "wiki_browser",
    "agent_dashboard"
  ],
  "compatible_features": [
    "feature_card_template",
    "source_reference_rules_template",
    "review_packet_template"
  ],
  "complexity": "medium",
  "dependencies": [
    "folder_skeleton_template",
    "review_packet_template",
    "decision_log_template"
  ],
  "example_projects": [
    "features",
    "codex_shell"
  ],
  "expected_outputs": [
    {
      "description": "Validated folder skeleton for Dex Feature Library Repo v1.",
      "name": "folder_skeleton",
      "type": "library_item"
    }
  ],
  "folder_skeleton": {
    "directories": [
      "features_tool",
      "library/items",
      "library/schemas",
      "tests",
      "docs",
      "state"
    ],
    "files": [
      {
        "path": "README.md",
        "purpose": "High-level repo entry point and launch commands."
      },
      {
        "path": "pyproject.toml",
        "purpose": "Packaging and CLI entrypoint."
      },
      {
        "path": "docs/contract.md",
        "purpose": "Lock metadata and CLI contracts."
      },
      {
        "path": "docs/roadmap.md",
        "purpose": "Track phases and what is intentionally deferred."
      },
      {
        "path": "docs/decisions.md",
        "purpose": "Record design decisions and reversal conditions."
      },
      {
        "path": "tests/test_library.py",
        "purpose": "Regression proof for schema, templates, and CLI."
      }
    ],
    "first_vertical_slice": [
      "Seed the library schema and core templates.",
      "Ship create-item, validate-library, list, and search CLI commands.",
      "Keep the repo queryable before adding richer UI or command-centre layers."
    ],
    "root": "."
  },
  "frameworks": [
    "cli"
  ],
  "implementation_notes": [
    "Treat this as the canonical repo shape, not a hand-wavy suggestion.",
    "Include docs and tests from the start so future generators do not bolt them on later."
  ],
  "known_failure_modes": [
    "Dex Feature Library Repo v1 is too vague to drive consistent repo setup.",
    "The skeleton ignores docs or tests, so later tasks reintroduce ad hoc sprawl."
  ],
  "languages": [
    "python"
  ],
  "logic_patterns": [
    "validation_pipeline",
    "search_index"
  ],
  "name": "Dex Feature Library Repo v1",
  "problem_solved": "Prevents repo sprawl when building Dex Feature Library Repo v1 by locking structure up front.",
  "required_inputs": [
    {
      "description": "Name of the repo shape the skeleton targets.",
      "name": "repo_shape",
      "required": true,
      "type": "string"
    },
    {
      "description": "Core surfaces or artifacts the structure must accommodate.",
      "name": "required_surfaces",
      "required": true,
      "type": "string_list"
    }
  ],
  "reusability": "high",
  "risk": "low",
  "schema_version": "feature_library_item.v1",
  "slug": "dex_feature_library_repo_v1",
  "status": "draft",
  "summary": "Concrete starter skeleton for a Dex-queryable feature library repo.",
  "tags": [
    "folder_skeleton",
    "repo_shape",
    "starter",
    "repo_foundation"
  ],
  "template": false,
  "type": "folder_skeleton",
  "ui_surfaces": [
    "wiki_browser",
    "universal_inspector"
  ],
  "when_not_to_use": [
    "Do not use when Dex Feature Library Repo v1 needs a domain-specific layout that would instantly violate the skeleton.",
    "Do not use when reorganizing a mature repo with already-locked structure."
  ],
  "when_to_use": [
    "Use when Dex Feature Library Repo v1 is a good starting repo shape for a new feature-library or app project.",
    "Use when structure should be created before code generation begins."
  ]
}
